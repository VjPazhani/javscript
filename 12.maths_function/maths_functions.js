// Maths functions
let c;
c=Math.PI;
c=Math.E;                    //Euler's Number 'e' is a numerical constant used in mathematical calculations. The value of e is 2.718281828459045…so on. 
c=Math.round(5.8);
c=Math.round(5.3);            //Just like pi(π), e is also an irrational number. It is described basically under logarithm concepts.
c=Math.floor(5.3);
c=Math.ceil(5.3);            //'e' is a mathematical constant, which is basically the base of the natural logarithm.
c=Math.sqrt(49);
c=Math.abs(-89);
c=Math.trunc(2.5678);
c=Math.pow(2,4);
c=Math.min(10,20,30,40,50);
c=Math.max(10,20,30,40,50);
c=Math.random();
c=Math.floor(Math.random()*50+1);
c=Math.sign(-25);
c=Math.sin(45);
c=Math.log(60);
console.log(c);